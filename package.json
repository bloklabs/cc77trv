{
  "name": "os3-concierge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "OS3 Concierge — an offline-first PWA for private family travel, dining, culture, and utility research.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "xray": "node scripts/xray-staging.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "node --check src/main.js && node --check src/lib/compat.js && node --check src/lib/normalize.js && node --check src/lib/store.js && node --check src/lib/sync.js && node --check src/lib/itinerary.js && node --check scripts/xray-staging.mjs && node --check vite.config.js"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "jsdom": "^24.1.0",
    "vite": "^5.4.10",
    "vite-plugin-pwa": "^0.20.5",
    "vitest": "^2.1.4"
  },
  "allowScripts": {
    "esbuild@0.21.5": true
  }
}
