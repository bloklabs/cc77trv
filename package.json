{
  "name": "os3-concierge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "OS3 Concierge \u2014 an offline-first PWA for private family travel, dining, culture, and utility research.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "xray": "node scripts/xray-staging.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "node --check src/main.js && node --check src/lib/compat.js && node --check src/lib/normalize.js && node --check src/lib/store.js && node --check src/lib/sync.js && node --check src/lib/itinerary.js && node --check scripts/xray-staging.mjs && node --check vite.config.js && node --check src/lib/voice.js && node --check src/voice-view.js",
    "test:browser": "playwright test"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "vite": "^8.2.2",
    "vite-plugin-pwa": "^1.3.0",
    "vitest": "^4.1.11"
  },
  "overrides": {
    "fast-uri": "3.1.6",
    "brace-expansion@^2.0.0": "2.1.4",
    "brace-expansion@^5.0.0": "5.0.9"
  }
}
